# Core dependencies for the Gemini Agent (Sentinel)

google-genai
google-adk

# Retrieval-Augmented Generation (RAG) components

sentence-transformers
faiss-cpu

# Utilities

python-dotenv # Used to load environment variables (.env file)
nest-asyncio # Utility for running async code in environments like Jupyter/Colab
